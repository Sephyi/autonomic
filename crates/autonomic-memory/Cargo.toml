[package]
name = "autonomic-memory"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "PostgreSQL memory store with tsvector FTS, pgvector semantic search, context assembly, and MEMORY.md sync"

[dependencies]

[lints]
workspace = true
